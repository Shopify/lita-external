require 'spec_helper'

describe Lita::External do
  it 'has a version number' do
    expect(Lita::External::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
